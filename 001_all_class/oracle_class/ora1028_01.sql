-- 주석
-- 실행단축키 : F9
-- 계정이름 앞에 c## 생략 코드
-- ALTER SESSION SET "_ORACLE_SCRIPT" = TRUE;

-- 사용자 생성
--CREATE USER ora_user IDENTIFIED BY 1111;

-- 권한 부여 : 접근권한, 리소스 권한, db생성 권한
--GRANT CONNECT, RESOURCE, DBA TO ora_user;

-- 권한 해제
--REVOKE CONNECT, RESOURCE, DBA FROM ora_user;

-- 사용자 삭제
--DROP USER ora_user;

